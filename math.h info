#include<stdio.h>
#include<math.h>
int main()
{
    double result,x=10.5;
    
   // result=log(x);
   // printf("log(%lf) = %lf",x,result);
   // result=exp(x);
   //printf("exp(%lf) = %lf",x,result);
    result=sin(x);
    printf("sin(%lf) = %lf",x,result);//cos,tan
    return 0;
}
